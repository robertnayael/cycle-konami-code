import { Button } from './'

describe('Button component', () => {

    it('should create', () => {

    })

    it('should render a <button> element', () => {

    })

    it('should emit `true` / `false` values on toggle on and off', () => {

    })

    it('should switch labels on toggle on and off', () => {

    })

})
